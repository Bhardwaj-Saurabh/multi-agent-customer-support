# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# .env is tracked (sanitized - no credentials) per reviewer request;
# local credential edits are hidden via `git update-index --skip-worktree .env`
.env.*
!.env.example

# Claude Code session data
.claude/

# Test / runtime artifacts
payload.json
response.json

project.md
